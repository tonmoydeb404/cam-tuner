export type StreamPatcherConfig = {
  aspectRatio?: number;
  zoom?: number;
  brightness?: number;
  saturation?: number;
  contrast?: number;
  mirror?: boolean;
  align?: "left" | "center" | "right";
};

export type StreamPatcherSize = {
  width: number;
  height: number;
};

